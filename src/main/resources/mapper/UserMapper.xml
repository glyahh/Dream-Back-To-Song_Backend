<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dbts.dreambacktosong_backend.mapper.UserMapper">

    <!--
      用户表映射
      对应表：users
    -->
    <resultMap id="UserResultMap" type="com.dbts.dreambacktosong_backend.domain.entity.User">
        <id property="id" column="id"/>
        <result property="phone" column="phone"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="bio" column="bio"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="UserResultMap">
        SELECT id, phone, nickname, avatar, bio, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findByPhone" resultMap="UserResultMap">
        SELECT id, phone, nickname, avatar, bio, created_at, updated_at
        FROM users
        WHERE phone = #{phone}
    </select>

    <select id="findByIds" resultMap="UserResultMap">
        SELECT id, phone, nickname, avatar, bio, created_at, updated_at
        FROM users
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <insert id="insert" parameterType="com.dbts.dreambacktosong_backend.domain.entity.User" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO users (phone, nickname, avatar, bio, created_at, updated_at)
        VALUES (#{phone}, #{nickname}, #{avatar}, #{bio}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.dbts.dreambacktosong_backend.domain.entity.User">
        UPDATE users
        SET
            nickname = #{nickname},
            avatar = #{avatar},
            bio = #{bio},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
