<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dbts.dreambacktosong_backend.mapper.UserFollowMapper">

    <!-- 关注关系表映射，对应表：user_follows -->
    <resultMap id="UserFollowResultMap" type="com.dbts.dreambacktosong_backend.domain.entity.UserFollow">
        <id property="id" column="id"/>
        <result property="followerId" column="follower_id"/>
        <result property="followingId" column="following_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="find" resultMap="UserFollowResultMap">
        SELECT id, follower_id, following_id, created_at
        FROM user_follows
        WHERE follower_id = #{followerId}
          AND following_id = #{followingId}
    </select>

    <insert id="insert" parameterType="com.dbts.dreambacktosong_backend.domain.entity.UserFollow" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO user_follows (follower_id, following_id, created_at)
        VALUES (#{followerId}, #{followingId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM user_follows
        WHERE follower_id = #{followerId}
          AND following_id = #{followingId}
    </delete>

    <select id="findByFollower" resultMap="UserFollowResultMap">
        SELECT id, follower_id, following_id, created_at
        FROM user_follows
        WHERE follower_id = #{followerId}
    </select>

</mapper>
