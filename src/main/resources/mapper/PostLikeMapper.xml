<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dbts.dreambacktosong_backend.mapper.PostLikeMapper">

    <!-- 帖子点赞表映射，对应表：post_likes -->
    <resultMap id="PostLikeResultMap" type="com.dbts.dreambacktosong_backend.domain.entity.PostLike">
        <id property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="find" resultMap="PostLikeResultMap">
        SELECT id, post_id, user_id, created_at
        FROM post_likes
        WHERE post_id = #{postId}
          AND user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.dbts.dreambacktosong_backend.domain.entity.PostLike" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO post_likes (post_id, user_id, created_at)
        VALUES (#{postId}, #{userId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM post_likes
        WHERE post_id = #{postId}
          AND user_id = #{userId}
    </delete>

</mapper>
